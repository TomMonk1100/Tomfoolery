import type { GameState } from '../core/state';
export interface HudRefs { fuel: HTMLElement; altitude: HTMLElement; descent: HTMLElement; drift: HTMLElement; attitude: HTMLElement; landing: HTMLElement; level: HTMLElement; score: HTMLElement; }
export function updateHud(refs: HudRefs, state: GameState): void { const ship = state.world.ship; refs.fuel.textContent = String(Math.ceil(ship.fuel)); refs.altitude.textContent = `${Math.max(0, Math.round(state.world.terrain.pad.y - ship.y))} u`; refs.descent.textContent = `${Math.round(Math.max(0, ship.vy))} u/s`; refs.drift.textContent = `${Math.round(Math.abs(ship.vx - state.world.terrain.pad.vx))} u/s`; refs.attitude.textContent = `${Math.round(ship.angle * 57.3)}°`; refs.landing.textContent = state.world.landing === 'approach' ? 'APPROACH' : state.world.landing.toUpperCase(); refs.level.textContent = String(state.run.level); refs.score.textContent = String(state.run.score).padStart(5, '0'); }
